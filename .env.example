# =============================================================================
# Claudio - Variables de Entorno
# =============================================================================
# Copia este archivo a .env y configura tus valores:
#   cp .env.example .env
#
# Nota: Cada canal (Telegram, Slack) tiene su propio .env.example con
# variables especificas. Este archivo contiene las variables compartidas.
# =============================================================================

# --- Workspace ---
# Directorio de trabajo donde Claudio ejecuta comandos
WORKSPACE_PATH=/path/to/claudio

# --- Claude Code CLI ---
# Ruta al ejecutable de Claude CLI (opcional, por defecto busca 'claude' en PATH)
CLAUDE_CLI_PATH=claude

# Herramientas permitidas (* = todas, recomendado para MCPs)
ALLOWED_TOOLS=*

# Saltar checks de permisos (recomendado para MCPs en modo no interactivo)
SKIP_PERMISSIONS=true

# Timeout maximo para ejecucion de comandos (en segundos, default: 1800 = 30 min)
COMMAND_TIMEOUT=1800

# --- Transcripcion de Voz (opcional) ---
# API key de OpenAI para Whisper (usado por bots de Telegram y Slack)
# Obten tu API key en: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# --- Telegram Bot ---
# Ver channels/telegram/.env.example para configuracion completa
TELEGRAM_BOT_TOKEN=
ALLOWED_USER_IDS=

# --- Slack Bot ---
# Ver channels/slack/.env.example para configuracion completa
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
SLACK_ALLOWED_USER_IDS=

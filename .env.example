# Token del bot de Telegram
# Obtén tu token desde @BotFather en Telegram
TELEGRAM_BOT_TOKEN=tu_token_aqui

# Ruta al ejecutable de Claude CLI (opcional, por defecto busca 'claude' en PATH)
CLAUDE_CLI_PATH=claude

# Directorio de trabajo donde se ejecutarán los comandos (opcional, por defecto el directorio actual)
WORKSPACE_PATH=/Users/ignaciodelacuba/Dev/claudio

# Herramientas permitidas automáticamente (para MCPs y herramientas sin prompts)
# '*' permite todas las herramientas (recomendado para usar MCPs)
# O puedes especificar herramientas específicas: "Read,Edit,Bash"
ALLOWED_TOOLS=*

# Saltar todos los checks de permisos (recomendado para usar MCPs en modo no interactivo)
# Los wildcards no funcionan con MCPs, así que esto es necesario para que funcionen
# true = usar --dangerously-skip-permissions (recomendado)
# false = usar --allowedTools (los wildcards no funcionan con MCPs)
SKIP_PERMISSIONS=true

# Seguridad: IDs de usuarios de Telegram autorizados para usar el bot
# Formato: "123456789,987654321" o un solo ID: "123456789"
# IMPORTANTE: Si está vacío, CUALQUIER usuario puede usar el bot (NO RECOMENDADO)
# Para obtener tu ID, envía /myid al bot después de iniciarlo
ALLOWED_USER_IDS=

# Buffer timeout: Tiempo en segundos para acumular salida antes de enviar a Telegram
# Reduce el spam de mensajes cortos. Valor recomendado: 1.5
BUFFER_TIMEOUT=1.5
